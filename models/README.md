# Folder models/

Taruh berkas-berkas model `.pkl` dan `.json` hasil unduhan dari sel ekspor Google Colab di folder ini:

1. `models_trained.pkl`
2. `models_class_weight.pkl`
3. `imputer.pkl`
4. `feature_names.pkl`
5. `config.json`

Setelah berkas dipindahkan ke folder ini, silakan push seluruh isi folder `streamlit_app` ke repositori GitHub Anda untuk dideploy ke Streamlit Cloud.
